# Crosscutting Concepts: Build Tooling

[← Architecture index](index.md)

The `scripts/` directory holds standalone build and quality-check tooling, including the `scripts/docs/` documentation-site build pipeline used to produce this very page. There is no data-fetch pipeline — the Projects section renders from the static, hand-curated `src/data/projects.config.js`.

*Full chapter content pending — will merge [scripts/overview.md](../scripts/overview.md) into this page.*
