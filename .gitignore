# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
# /build is CRA's output dir, /dist is Vite's — both listed while the two
# build systems coexist; /build is dropped when react-scripts is removed.
/build
/dist

# misc
.env
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local generated artifacts
eslint-output.txt
*.log
/.cache
/.vscode
/.idea

