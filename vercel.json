{
  "github": {
    "productionBranch": "main"
  },
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "ignoreCommand": "[ \"$VERCEL_GIT_COMMIT_REF\" = \"gh-pages\" ] && exit 0; git diff --quiet HEAD^ HEAD -- . ':(exclude)docs' ':(exclude)*.md' && exit 0 || exit 1"
}
