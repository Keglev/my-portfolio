const images = require('./images');
const techs = require('./techs');
const docs = require('./docs');
const helpers = require('./helpers');

module.exports = Object.assign({}, images, techs, docs, helpers);
